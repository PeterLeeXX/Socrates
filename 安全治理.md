# 安全治理机制说明

本文说明当前项目的安全治理机制。这里的“安全治理”主要围绕工具调用展开：模型不能直接操作文件、执行命令或访问网络，而是必须通过 `ToolRegistry` 分发工具调用；每次工具调用都会经过输入校验、权限判定、路径约束、用户确认或自动拒绝等步骤。

整体上，安全治理可以分成几类触发场景：

1. 程序启动时解析权限模式。
2. 工具真正执行前做统一权限检查。
3. Bash、文件、网络等高风险工具做专项安全检查。
4. 交互模式下把需要确认的操作交给用户。
5. 非交互模式下自动拒绝需要确认的操作。
6. 子代理运行时继承或收紧父级权限。
7. 工具内部再做防绕过的二次检查。

## 1. 启动阶段：确定本次会话的权限模式

安全治理的第一步发生在 CLI 启动阶段。

入口在 `src/cli.py`：

- `main()`
- `_build_parser()`
- `_resolve_permission_state(args)`

命令行支持这些权限相关参数：

- `--permission-mode`
- `--dangerously-skip-permissions`
- `--allow-dangerously-skip-permissions`

可用权限模式定义在 `src/permissions/types.py`：

- `default`
- `plan`
- `acceptEdits`
- `bypassPermissions`
- `dontAsk`

启动时的流程是：

用户启动 CLI  
解析命令行参数  
调用 `_resolve_permission_state(args)`  
如果用户请求跳过权限检查，先运行危险模式安全门  
根据 CLI 参数和设置解析出最终权限模式  
把结果写入 `args._resolved_permission_mode`  
后续 REPL 统一使用这个权限模式

其中，`src/permissions/modes.py` 的 `initial_permission_mode_from_cli()` 负责决定优先级：

1. 如果传了 `--dangerously-skip-permissions`，模式变成 `bypassPermissions`。
2. 否则如果传了 `--permission-mode`，使用该模式。
3. 否则使用设置中的默认权限模式。
4. 最后 fallback 到 `default`。

## 2. 危险跳权模式的启动安全门

如果用户传入 `--dangerously-skip-permissions` 或 `--allow-dangerously-skip-permissions`，系统会先调用：

`src/permissions/dangerous_safety.py`

核心函数：

- `enforce_dangerous_skip_permissions_safety(...)`
- `is_sandbox_environment()`

它的目标是防止一个高权限进程在非沙箱环境下直接绕过所有权限检查。

流程是：

用户请求危险跳权  
检查当前是否运行在 root/sudo 权限下  
检查是否处于沙箱环境  
如果是 root 且不在沙箱中，则直接拒绝启动  
否则允许继续启动

沙箱环境通过环境变量判断：

- `IS_SANDBOX`
- `CLAUDE_CODE_BUBBLEWRAP`

在 Windows 上，这个 root 检查会跳过，因为代码里认为 Windows 没有同样的 `uid 0` 概念。

## 3. ToolContext：安全治理的运行时上下文

工具运行时的安全状态集中放在 `src/tool_system/context.py` 的 `ToolContext` 中。

关键字段包括：

- `workspace_root`: 当前工作区根目录。
- `cwd`: 当前工具执行目录。
- `permission_context`: 当前权限上下文。
- `additional_working_directories`: 额外允许访问的工作目录。
- `read_file_fingerprints`: 已读取文件的指纹，用于写入前 stale check。
- `allow_docs`: 是否允许创建或编辑文档文件。
- `permission_handler`: 当工具需要用户确认时，调用这个 handler。
- `plan_mode`: 是否处于计划模式。

其中最重要的路径治理函数是：

`ensure_allowed_path(path)`

它的流程是：

接收工具传入的路径  
如果是相对路径，则基于当前 `cwd` 解析  
如果是绝对路径，则直接 resolve  
取出允许的根目录列表  
检查目标路径是否位于 `workspace_root` 或额外允许目录之内  
如果在允许范围内，返回规范化后的路径  
如果越界，抛出 `ToolPermissionError`

这意味着大多数文件工具都不能随意读写工作区外部路径，除非该路径被显式加入额外允许目录。

## 4. 工具调用的统一治理入口

模型发出的工具调用最终会进入：

`src/tool_system/registry.py`

核心函数：

- `ToolRegistry.dispatch(call, context)`

这是所有工具执行前的统一入口。

完整流程是：

模型发起工具调用  
`ToolRegistry` 根据工具名查找工具对象  
如果工具不存在，返回错误  
调用 `context.ensure_tool_allowed(tool.name)` 检查工具是否被 deny rule 阻止  
调用 `validate_json_schema()` 校验输入结构  
如果工具定义了 `validate_input()`，继续做工具级输入校验  
调用 `has_permissions_to_use_tool()` 做权限判定  
如果判定为 deny，返回权限错误  
如果判定为 ask，调用 `handle_permission_ask()` 请求用户确认  
如果用户拒绝或没有可用 handler，返回权限错误  
如果允许，执行工具的 `call()`  
把工具结果返回给查询循环

这个入口非常关键：它把“模型想做什么”和“系统实际允许做什么”分开。模型只能提出工具调用，真正是否执行由 registry 和 permissions 模块决定。

## 5. 输入结构校验

工具执行前会先调用：

`src/tool_system/schema_validation.py`

核心函数：

- `validate_json_schema(value, schema, root_name="input")`

它会检查：

- 输入是否是对象。
- 必填字段是否存在。
- 字段类型是否符合 schema。
- 是否出现了 `additionalProperties=False` 下不允许的字段。
- enum、oneOf、anyOf 是否匹配。

如果校验失败，会抛出 `ToolInputError`。

这一步的作用是：阻止模型把任意结构塞给工具，减少工具内部处理异常输入或意外字段的风险。

## 6. 权限判定总流程

权限判定核心在：

`src/permissions/check.py`

主要函数：

- `has_permissions_to_use_tool(...)`
- `has_permissions_to_use_tool_inner(...)`
- `check_rule_based_permissions(...)`
- `handle_permission_ask(...)`

判定流程是：

先检查 deny rule  
如果命中 deny，直接拒绝  
再检查 ask rule  
如果命中 ask，要求用户确认  
调用工具自己的 `check_permissions()`  
如果工具返回 deny，拒绝  
如果工具返回 ask，根据当前模式决定是否询问  
如果当前是 `bypassPermissions`，直接允许  
如果当前是 plan 且允许 bypass，也允许  
检查 always allow rule  
检查 Bash 内容级 allow rule  
如果工具结果仍是 passthrough，则默认 ask  
如果工具结果是 allow，则允许  

这个流程体现了几个优先级：

1. 明确 deny 最高优先级。
2. 明确 ask 会强制进入确认流程。
3. 工具自己的安全检查可以要求确认或拒绝。
4. bypass 模式会跳过大部分 ask/passthrough。
5. 默认模式下，未明确允许的高风险工具通常会走 ask。

## 7. dontAsk 和无交互场景的治理

`has_permissions_to_use_tool()` 在外层还会处理两个特殊情况。

第一种是 `dontAsk` 模式：

如果某个操作需要 ask，而当前模式是 `dontAsk`，系统不会弹确认，而是直接拒绝。

第二种是 `should_avoid_permission_prompts=True`：

如果当前上下文不能安全地打断用户确认，例如后台子代理或某些非交互环境，那么任何需要 ask 的操作都会被拒绝。

这避免了系统在无人值守的管道、后台任务或子代理里卡住等待确认。

## 8. 用户确认处理

用户确认逻辑在：

`src/permissions/handler.py`

核心函数：

- `handle_permission_ask(tool_name, decision, handler)`

流程是：

权限判定返回 ask  
如果没有 `permission_handler`，直接拒绝  
如果有 handler，向 handler 传入工具名、提示信息和建议  
handler 返回允许或拒绝  
允许则返回 `PermissionAllowDecision`  
拒绝则返回 `PermissionDenyDecision`

在交互式 REPL 中，`ToolContext.permission_handler` 会接到用户确认流程。

在没有可用 `permission_handler` 的上下文中，ask 会被拒绝，而不会阻塞等待用户输入。后台子代理还会显式设置 `should_avoid_permission_prompts=True`，因此需要确认的操作会直接失败。

## 9. Bash 命令安全治理

Bash 是最高风险工具之一，它的治理分成三层。

第一层是权限检查：

`src/tool_system/tools/bash/bash_tool.py`

相关函数：

- `_bash_check_permissions(...)`
- `_bash_validate_input(...)`
- `_bash_call(...)`

`_bash_check_permissions()` 会调用：

`src/permissions/bash_security.py`

核心函数：

- `analyze_bash_command(command)`
- `check_bash_command_safety(command, cwd=None, allowed_directories=None)`

流程是：

解析 Bash 命令  
如果命令结构太复杂，要求确认  
分类每个简单命令的安全等级  
汇总出整体安全等级  
如果是 dangerous、destructive、unknown，要求确认  
如果是 safe 或 read_only，则不额外要求确认  

安全等级包括：

- `safe`
- `read_only`
- `write`
- `destructive`
- `dangerous`
- `unknown`

第二层是输入校验。

`_bash_validate_input()` 会阻止长时间阻塞式 sleep 模式。如果检测到阻塞 sleep，会要求使用后台运行，避免工具调用长时间挂起。

第三层是执行前硬编码拦截。

`_bash_call()` 内部有 `_HARDCODED_DANGEROUS_PATTERNS`，即使绕过了 registry 的权限检查，直接调用 `_bash_call()` 时也会拒绝明显危险的命令，例如：

- `sudo`
- `shutdown`
- `reboot`
- `mkfs`
- `dd if=...`
- `rm -rf /`
- fork bomb

这是一层防绕过的纵深防御。

Bash 还有执行目录治理：

- 如果显式传入 `cwd`，必须是绝对路径。
- `cwd` 会通过 `context.ensure_allowed_path()` 检查。
- 如果命令改变了目录，系统只会把最终 cwd 更新为允许目录内的位置。

## 10. 文件读取治理

文件读取工具在：

`src/tool_system/tools/read.py`

核心函数：

- `_read_call(...)`

读取治理包括：

1. 路径必须是非空字符串。
2. 禁止读取会阻塞或无限输出的设备路径，例如 `/dev/zero`、`/dev/random`、`/dev/stdin`。
3. 禁止读取不支持的二进制扩展文件。
4. 通过 `context.ensure_allowed_path()` 限制路径必须在允许工作目录内。
5. 如果路径不存在，返回带 cwd 和相似路径建议的错误。
6. 禁止把目录当文件读。
7. offset 和 limit 必须是正整数。
8. 对大文件做预读大小限制。
9. 对输出做 token 估算限制，超过限制则要求用户用 offset/limit 或 Grep 缩小范围。
10. 读取成功后记录 `read_file_fingerprints`。

读取指纹用于后续写入和编辑时判断文件是否被用户或其他进程改过。

此外，Read 还有去重机制：如果同一个文件已经完整读过，且文件没有变化，再次读取时会返回“文件未变化”的 stub，减少上下文浪费。

## 11. 文件写入治理

文件写入工具在：

`src/tool_system/tools/write.py`

核心函数：

- `_validate_input(...)`
- `_check_permissions(...)`
- `_write_call(...)`

写入前的治理流程是：

检查 `file_path` 和 `content` 类型  
拒绝用 Write 写 `.ipynb`，要求使用 NotebookEdit  
解析路径并检查是否在允许目录内  
如果是新文件，允许进入后续流程  
如果是已有文件，必须已经被 Read 完整读取过  
如果文件只被部分读取过，拒绝写入  
如果文件读取后又被用户或其他进程修改过，拒绝写入  
如果目标是 Markdown 文档且 `allow_docs` 未启用，则要求确认  
真正写入前再次检查文件未变化  
写入后更新 read fingerprint  
返回结构化 diff

这里最重要的安全点是“写前必须读且未变化”。它避免模型基于过期上下文覆盖用户的新改动。

## 12. 文件编辑治理

文件编辑工具在：

`src/tool_system/tools/edit.py`

核心函数：

- `_check_permissions(...)`
- `_edit_call(...)`

编辑治理包括：

1. 目标路径必须在允许目录内。
2. `.ipynb` 文件不能用普通 Edit 修改。
3. 如果 `old_string` 为空，只能创建不存在的新文件。
4. 如果编辑已有文件，文件必须存在且是普通文件。
5. 文件大小不能超过内部限制。
6. 文件必须已经被 Read 读取过，且从读取到编辑期间没有变化。
7. `old_string` 必须能在文件里找到。
8. 如果 `old_string` 出现多次且未指定 `replace_all`，拒绝编辑。
9. 非 Markdown 文件会剥离新内容的行尾空白。
10. Markdown 文档编辑在 `allow_docs` 未启用时需要确认。

Edit 也会返回结构化 diff，方便上层 UI 或对话记录展示变更。

## 13. 自动编辑路径安全分类

路径安全辅助逻辑在：

`src/permissions/filesystem.py`

核心函数：

- `check_path_safety_for_auto_edit(file_path, cwd=None)`
- `check_write_permission_for_path(...)`
- `check_read_permission_for_path(...)`

它会把一些路径视为需要确认或不宜自动编辑：

受保护文件包括：

- `.env`
- `.npmrc`
- `.pypirc`
- `.netrc`
- `.aws/credentials`
- `.ssh/config`
- `.kube/config`
- `.gitconfig`
- `Makefile`

受保护目录包括：

- `.git`
- `.ssh`
- `.gnupg`
- `.config`
- `.vscode`
- `.idea`

受保护 lockfile 包括：

- `package-lock.json`
- `yarn.lock`
- `pnpm-lock.yaml`
- `poetry.lock`
- `Cargo.lock`
- `go.sum`

这些文件或目录并不一定完全禁止修改，但通常会触发 ask，要求用户明确确认。

需要注意的是，当前 `auto_mode_classify()` 会使用 `check_path_safety_for_auto_edit()`，但从现有主工具分发路径看，`auto_mode_classify()` 没有接入 `ToolRegistry.dispatch()` 的主权限流程。也就是说，这部分更像自动模式分类能力，是否生效取决于调用方是否使用它。

## 14. 网络访问治理

WebFetch 工具在：

`src/tool_system/tools/web_fetch.py`

核心函数：

- `_validate_url(url)`
- `_fetch_with_redirect_handling(url)`
- `_web_fetch_call(tool_input, context)`

网络治理包括：

1. URL 长度不能超过限制。
2. HTTP 会自动升级为 HTTPS。
3. 只允许 http/https，实际校验后只接受 https。
4. URL 不能带 username/password。
5. hostname 必须有效。
6. 禁止 localhost。
7. 禁止解析到 private、loopback、link-local、reserved、multicast 地址。
8. 限制重定向次数。
9. 只有同 scheme、同端口、同主机的重定向会自动跟随。
10. 如果重定向到不同 host，只返回提示，要求用户显式发起新的请求。
11. 响应最多读取约 `1_000_000` bytes。
12. 返回给模型的内容超过 `100_000` 字符会截断。
13. 有 15 分钟 LRU 缓存，避免重复抓取。

这部分主要防 SSRF、本地网络访问、凭据 URL、跨域重定向偷换目标，以及超大网页导致上下文爆炸。

需要注意的是，WebFetch 的文件注释提到 prompt 参数用于二次模型处理，但当前实现实际只是把 `prompt` 和网页内容拼接返回，并没有看到独立的小模型安全总结流程。

## 15. 文档文件写入治理

Write 和 Edit 都有一个特殊规则：默认不允许直接创建或编辑 Markdown 文档。

相关字段：

- `ToolContext.allow_docs`

相关逻辑：

- `src/tool_system/tools/write.py`
- `src/tool_system/tools/edit.py`

如果目标后缀是 `.md` 或 `.markdown`，且 `allow_docs` 为 `False`，工具会返回 ask，需要用户确认。

在 REPL 中，用户确认某些文档写入操作后，可能会启用 `allow_docs`。在 `bypassPermissions` 模式下，REPL 初始化时也会放开 `allow_docs`。

这是一种专门防止模型未经用户要求乱写说明文档、README 或 Markdown 文件的治理。

## 16. 无交互权限请求治理

当前代码已经删除独立的非交互 CLI 入口，因此没有对应的单独运行路径。无交互治理主要由通用权限字段承担，而不是由单独入口模块承担。

核心机制是：

如果 `ToolContext.permission_handler` 不存在，需要 ask 的操作会被拒绝  
如果 `ToolPermissionContext.should_avoid_permission_prompts=True`，需要 ask 的操作会被拒绝  
如果权限模式是 `dontAsk`，需要 ask 的操作会被拒绝  
后台子代理会设置 `should_avoid_permission_prompts=True`，避免后台任务弹出确认或阻塞主流程  

因此，“无交互”现在是一种权限上下文状态，而不是一个独立 CLI 入口。

## 17. 子代理安全治理

子代理相关逻辑在：

- `src/agent/run_agent.py`
- `src/agent/subagent_context.py`
- `src/agent/agent_tool_utils.py`
- `src/agent/constants.py`

子代理安全治理有三层。

第一层是权限模式继承。

`resolve_permission_mode()` 的规则是：

如果父上下文是 `bypassPermissions`、`acceptEdits` 或 `dontAsk`，子代理继承父级模式  
否则如果 agent 定义了自己的 permission mode，则使用 agent 的模式  
否则沿用父级模式

第二层是工具过滤。

`filter_tools_for_agent()` 会过滤子代理可用工具：

- 所有 agent 默认不能使用 `Agent`、`EnterPlanMode`、`ExitPlanMode`、`AskUserQuestion`、`TaskStop` 等工具。
- 自定义 agent 还会应用额外限制。
- 异步后台 agent 只能使用白名单工具。

异步 agent 允许工具白名单包括：

- `Read`
- `WebSearch`
- `TaskCreate`
- `TaskGet`
- `TaskList`
- `TaskUpdate`
- `Grep`
- `WebFetch`
- `Glob`
- `Bash`
- `Edit`
- `Write`
- `Skill`
- `StructuredOutput`
- `EnterWorktree`
- `ExitWorktree`

第三层是上下文隔离。

`create_subagent_context()` 会为子代理创建独立 `ToolContext`：

- 新的 abort controller，必要时和父级联动。
- 新的 read file fingerprint cache。
- 新的 tasks、outbox。
- 默认不共享 permission handler。
- 后台 agent 会设置 `should_avoid_permission_prompts=True`。

这意味着后台子代理如果遇到需要用户确认的工具调用，会直接被拒绝，而不是弹出确认或阻塞主流程。

## 18. Plan Mode 治理

计划模式工具在：

`src/tool_system/tools/plan_mode.py`

核心工具：

- `EnterPlanMode`
- `ExitPlanMode`

`EnterPlanMode` 会把 `context.plan_mode` 设为 `True`，提示模型先探索和制定方案。

`ExitPlanMode` 要求当前确实处于 plan mode，否则抛出权限错误。退出时可以把计划写入文件，写入路径也会经过 `context.ensure_allowed_path()`。

从当前代码看，plan mode 本身更多是行为约束和流程标记；实际禁止写入主要还是依赖权限模式、工具过滤和具体工具校验。

## 19. MCP 和工具池治理

工具池组装在：

`src/tool_system/registry.py`

相关函数：

- `filter_tools_by_deny_rules(...)`
- `get_tools(...)`
- `assemble_tool_pool(...)`
- `get_merged_tools(...)`

工具展示给模型前，会先根据 deny rules 过滤。MCP 工具也会经过 deny rule 过滤。

权限自动分类里还有一个保守策略：`auto_mode_classify()` 默认认为 MCP 工具需要显式批准。不过如前所述，该函数是否参与主流程取决于调用方。

## 20. 当前安全治理的实际主流程

把主流程串起来，可以这样理解：

程序启动  
解析权限模式和危险跳权参数  
创建 REPL 运行环境  
创建 `ToolContext` 和 `ToolPermissionContext`  
模型产生工具调用  
`ToolRegistry.dispatch()` 接收调用  
检查工具是否存在  
检查工具是否被 deny  
校验 JSON schema  
运行工具级输入校验  
运行统一权限判定  
必要时请求用户确认  
确认失败或无确认能力则拒绝  
确认通过或模式允许则执行工具  
工具内部再次检查路径、文件状态或命令危险性  
返回工具结果

这个流程的核心思想是：模型的意图不等于系统授权。每个真实副作用都必须通过工具系统和权限系统落地。

## 21. 当前实现中的风险点和注意事项

第一，`ToolContext` 的默认 `permission_context` 是 `bypassPermissions`。正常 CLI 路径会显式传入解析后的权限模式，但如果测试或其他代码直接构造 `ToolContext()` 而不覆盖权限上下文，默认会偏宽松。

第二，`WriteTool` 定义了 `backfill_observable_input`，用于把路径展开成绝对路径，避免 hook allowlist 被 `~` 或相对路径绕过。但当前 `ToolRegistry.dispatch()` 主流程没有调用 `tool.backfill_observable_input`。不过 Write 最终仍会通过 `ensure_allowed_path()` 做路径限制。

第三，`auto_mode_classify()` 和 `filesystem.py` 里的部分自动安全分类能力没有直接接入主 dispatch 流程。它们是可用能力，但不能假设所有主路径都会自动使用。

第四，WebFetch 有基本 SSRF 和私网防护，但并没有完整的“网页内容提示注入隔离”机制。网页内容会作为工具结果返回给模型，因此模型仍需要遵守系统提示，不应把网页中的指令当成开发者或用户指令。

第五，Bash 的复杂命令分类依赖 parser 和命令分类表。虽然有硬编码危险模式兜底，但 shell 语言非常灵活，仍应把 Bash 视为高风险工具。

第六，文档写入默认 ask 是治理规则，不是绝对禁止。用户确认或 bypass 模式可以放开。

第七，非交互模式下需要确认的操作会自动拒绝，这更安全，但也可能导致一些自动化任务无法完成，除非显式配置 bypass 或允许工具。

## 22. 关键函数速查表

| 文件 | 函数 / 类 | 功能 |
|---|---|---|
| `src/cli.py` | `_resolve_permission_state()` | 启动时解析权限模式 |
| `src/permissions/modes.py` | `initial_permission_mode_from_cli()` | 根据 CLI 和设置决定初始权限模式 |
| `src/permissions/dangerous_safety.py` | `enforce_dangerous_skip_permissions_safety()` | root 非沙箱下禁止危险跳权 |
| `src/tool_system/context.py` | `ToolContext` | 工具运行时安全上下文 |
| `src/tool_system/context.py` | `ensure_allowed_path()` | 限制路径必须位于允许工作目录内 |
| `src/tool_system/context.py` | `ensure_tool_allowed()` | 根据 deny rules 阻止工具 |
| `src/tool_system/registry.py` | `ToolRegistry.dispatch()` | 工具调用统一安全入口 |
| `src/tool_system/schema_validation.py` | `validate_json_schema()` | 工具输入 schema 校验 |
| `src/permissions/check.py` | `has_permissions_to_use_tool()` | 权限判定总入口 |
| `src/permissions/check.py` | `has_permissions_to_use_tool_inner()` | deny、ask、tool check、bypass、allow 的具体判定 |
| `src/permissions/handler.py` | `handle_permission_ask()` | 处理用户确认 |
| `src/permissions/bash_security.py` | `analyze_bash_command()` | 分析 Bash 命令安全等级 |
| `src/permissions/bash_security.py` | `check_bash_command_safety()` | 对危险 Bash 命令要求确认 |
| `src/tool_system/tools/bash/bash_tool.py` | `_bash_call()` | Bash 执行和硬编码危险命令兜底 |
| `src/tool_system/tools/read.py` | `_read_call()` | 文件读取安全检查 |
| `src/tool_system/tools/write.py` | `_validate_input()` | 写文件前 stale check 和类型检查 |
| `src/tool_system/tools/write.py` | `_write_call()` | 写文件执行前二次校验 |
| `src/tool_system/tools/edit.py` | `_edit_call()` | 编辑文件前路径、读取状态、唯一匹配校验 |
| `src/permissions/filesystem.py` | `check_path_safety_for_auto_edit()` | 识别敏感文件、目录、lockfile |
| `src/tool_system/tools/web_fetch.py` | `_validate_url()` | URL、私网、凭据和协议安全检查 |
| `src/agent/run_agent.py` | `resolve_permission_mode()` | 子代理权限模式继承 |
| `src/agent/agent_tool_utils.py` | `filter_tools_for_agent()` | 子代理工具过滤 |
| `src/agent/subagent_context.py` | `create_subagent_context()` | 子代理上下文隔离 |
